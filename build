#!/bin/bash

for file in *.ino ; do
	mv "$file" "$file"_
done

cat *.ino_ > temp.ino

if [[ $# -eq 0 ]] ;then
	make all
else
	make "$@"
fi
retval=$?

rm temp.ino

for file in *.ino_ ; do
	mv "$file" "${file%%_}"
done

exit $retval
